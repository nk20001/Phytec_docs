#include<stdio.h>
int main()
{
    int a, even, odd;
    printf("enter any number: ");
    scanf("%d",&a);
    if (a%2==0)
    printf("no is even %d: ",even);
    else 
    printf("no is odd :%d",odd);
    return 0;

}