#include<stdio.h>
int main()
{
	int intt;
	float floatt;
	char chart;
	double doublet;
	printf("enter the size of:%ld",sizeof(intt));
	printf("enter the size of:%ld",sizeof(floatt));
	printf("enter the size of:%ld",sizeof(chart));
	printf("enter the size of :%ld",sizeof(doublet));
	return 0;
}

